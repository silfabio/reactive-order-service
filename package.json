{
  "name": "reactive-order-service",
  "version": "0.0.1-SNAPSHOT",
  "private": true,
  "scripts": {
    "sonar:local": "./gradlew build && docker run --rm -v \"$(pwd):/usr/src\" --network=host sonarsource/sonar-scanner-cli"
  }
}
