{
  "name": "reactive-order-service",
  "version": "0.0.1-SNAPSHOT",
  "private": true,
  "scripts": {
    "pre-sonar:local": "./gradlew build",
    "sonar:local": "docker run --rm -v \"$PWD:/usr/src\" --network=host sonarsource/sonar-scanner-cli"
  }
}
