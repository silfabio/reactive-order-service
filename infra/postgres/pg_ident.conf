# Maps the CN of the Vault-issued Order Service client certificate to the
# "order_service" Postgres role used in pg_hba.conf.

# MAPNAME              SYSTEM-USERNAME          PG-USERNAME
order_service_map      order-service.client     order_service
