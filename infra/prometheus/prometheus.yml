---
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'reactive-order-service'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          application: 'Reactive Order Service'
          environment: 'local'
